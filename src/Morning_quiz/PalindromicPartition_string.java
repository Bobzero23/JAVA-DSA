package Morning_quiz;

/*main class*/
public class PalindromicPartition_string {

    /*recursive method*/
    public String partition(String str){
        if (str.isEmpty()){
            return "No String found";
        }
        if (str.length() == 1){
            return str;
        }

        /*if the string length is greater than one leave space between each letter*/
        /*first we have to find the palindromic partitions*/
        /*then we separate the word from the other words*/
        return  "";

    }

    /*main method*/
    public static void main(String[] args) {

    }
}
