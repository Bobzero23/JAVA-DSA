Stack
+ Last In first out
+ Example of the usage of the stack is the back button, it is always useful when the last object matters

Stack Methods
+ Push
+ Pop -> deletes the last element
+ Peek -> Returning the last element
+ isFull
+ isEmpty

Stack with LinkedList
Note: The insert and delete methods of linked list with stack are different from the original one because in
insertion we always insert in the beginning and we delete in the beginning.

